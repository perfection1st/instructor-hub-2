import {Login} from '../components/Login';
import {Register} from '../components/Register';

export const Account = () => {
  return(
    <>
      {// If not logged in login component
        // If Logged in, direct to homepage
        // If no account, sign up
      }
    </>
  );
}